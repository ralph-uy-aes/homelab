# IG.09 - Subdomain Enumeration with Sublist3r

### Sublist3r
- Python tool designed to enumerate subdomains of websites using OSINT
    - Helps pentesters and bug hunters to get subdomains
- Syntax: `python sublist3r.py -d hostname`
- Sometimes Google will block sublist3r requests, so use a VPN in order to bypass the block